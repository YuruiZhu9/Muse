# MuseRecSys - LLM状态感知的多阶段推荐系统推理管线
# Requirements file for dependency management
#
# 快速安装: pip install -r requirements.txt
# 安装LLM语义召回功能: pip install -r requirements-llm.txt

# ============================================================================
# 核心依赖 (必需)
# ============================================================================
numpy>=1.21.0
pandas>=1.3.0
torch>=2.0.0
scikit-learn>=1.0.0
scipy>=1.7.0

# ============================================================================
# 可选依赖 (用于LLM语义召回)
# ============================================================================
# 安装方法: pip install faiss-cpu
# faiss-cpu>=1.7.0
# 如果有CUDA，也可以使用: pip install faiss-gpu

# ============================================================================
# 说明
# ============================================================================
# 1. 如果只运行基础推荐功能（不含LLM语义召回），只需要核心依赖
# 2. LLM语义召回功能需要 faiss-cpu 或 faiss-gpu
# 3. 系统会自动检测 faiss 是否安装，并相应启用/禁用 LLM 功能
